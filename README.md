Amuser is a client for The Muse's [public jobs API](https://www.themuse.com/developers/api/v2).

To run locally:

- clone the repo
- `bundle install`
- `bin/rails s`
- `localhost:3000`

Tests: `rspec spec/`
